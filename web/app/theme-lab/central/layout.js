export const metadata = {
  title: "เทมเพลตกลาง | Theme Lab | HUAJAIY",
  description: "ดูเลย์เอาต์หัวใจ + ฟุตเตอร์แบบกลางก่อนนำไปใช้หน้าอื่น"
};

export default function ThemeLabCentralLayout({ children }) {
  return children;
}
